/** Max likes (claps) one visitor can give per article. */
export const MAX_LIKES_PER_VISITOR = 13

/** Count at most one view per visitor per page per day. */
export const VIEW_DEDUP_TTL_MS = 24 * 60 * 60 * 1000
